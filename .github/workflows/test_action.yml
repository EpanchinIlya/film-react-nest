name: test-workflow
on:
  push:
    branches:
      - '**'
    tags:
      - '**'
jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello from GitHub Actions!"
